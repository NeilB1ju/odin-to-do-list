import tabsListeners from "./tabs";

tabsListeners();

//Make the inbox selected by default (on page load)
//Try creating tasks and see if the memory is maintained upon removing the child nodes
//Style the tasks 
//Add the projects section